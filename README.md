# Lambda Layer Puppeeteer

Just `make` and upload the resulting `modules.zip` to your AWS Lambda as a layer.

This layer includes the following modules, which can be useful for scraping tasks:
- `puppeteer-core`
- `node-fetch`
- `chrome-aws-lambda`
